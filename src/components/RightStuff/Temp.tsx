import { useStyles } from "../styles/RightStuff";
import EmojiPicker from 'emoji-picker-react';
const Temp = () => {

  const classes=useStyles();

  return <div className={classes.temp}>temp</div>;
};

export default Temp;
