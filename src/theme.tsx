import { createTheme, colors } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      default: "#17181c",
    },
    text: {
      primary: "#ffffff",
      secondary:"#898b8e"
    },
    secondary: {
      main:"#26262e",
    },
  },
});

export default theme;
